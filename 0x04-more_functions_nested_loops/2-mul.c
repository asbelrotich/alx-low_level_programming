#include "main.h"
/**
 * mul - multiplies two integers
 * @x: the first number ti multiply
 * @y: the second number to be multiplied
 * Return: The result of multiplication between x and y
 */
int mul(int x, int y)
{
	return (x * y);
}
